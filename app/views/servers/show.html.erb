<p id="notice"><%= notice %></p>

<H3>Server:<%= @server.name %></H3>
<%= render :partial => 'tree_level', :locals => {:items => @server.show_tree.children} %>

<%= link_to 'Edit', edit_server_path(@server) %> |
<%= link_to 'Back', servers_path %>
