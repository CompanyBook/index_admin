<h2>Path <small><%= @hdfs_source_path %></small></h2>

<table class="table table-striped">
  <tr>
    <th>Name</th>
    <th></th>
  </tr>

  <% @servers.each do |server| %>
      <tr>
        <td><%= server.name %></td>
        <td><%= link_to('Show', server_path(server, :hdfs_source_path => @hdfs_source_path)) %></td>
      </tr>
  <% end %>
</table>

<br/>

<%= link_to 'New Server', new_server_path, :class=>'btn' %>
